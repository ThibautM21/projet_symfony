{% extends 'base.html.twig' %}

{% block title %}Ajout d'un article
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="my-4">Ajouter un article</h1>
		{# {{ form(form) }} #}
		{{ form_start(form)}}
		{{form_widget(form)}}
		<div class="text-center">
			<button class="btn btn-primary btn-sm">Ajouter l'article</button>
		</div>
		{{form_end(form)}}
	</div>
{% endblock %}
